import { GestionDomiciliosVistaUnica } from "./components/GestionDomiciliosVistaUnica";

/**
 * Gestión Domicilios — vista única operativa PR6C.6 (mapa + lista).
 */
const GestionarDomiciliosContainer = () => {
  return <GestionDomiciliosVistaUnica />;
};

export default GestionarDomiciliosContainer;
